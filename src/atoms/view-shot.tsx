// import { Theme } from '@/themes';
// import { BoxProps, createBox } from '@shopify/restyle';
// import * as React from 'react';
// import NativeViewShot, { ViewShotProperties } from 'react-native-view-shot';

// const ViewShotRestyle = createBox<Theme, ViewShotProperties>(NativeViewShot);

// interface ThemePickerHandle {
//     capture: () => Promise<any> | undefined;
// }
// type ViewShot = ThemePickerHandle;

// const ViewShot = React.forwardRef(
//     (props: React.PropsWithChildren<BoxProps<Theme> | any>, ref: React.ForwardedRef<ThemePickerHandle | undefined>) => (
//         <ViewShotRestyle ref={ref} flex={1} {...props}>
//             {props.children}
//         </ViewShotRestyle>
//     )
// );

// export default ViewShot;
