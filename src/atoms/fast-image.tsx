// import * as React from 'react';
// import { BoxProps, createBox } from '@shopify/restyle';
// import { Theme } from '@/themes';
// import * as NativeFastImage from 'react-native-fast-image';

// const FastImageStyled = createBox<Theme, NativeFastImage.FastImageProps>(NativeFastImage.default);

// const FastImage: React.FC<React.PropsWithChildren<BoxProps<Theme>> & NativeFastImage.FastImageProps> = (props) => (
//     <FastImageStyled width={32} height={32} resizeMode={'contain'} {...props} />
// );

// export default FastImage;
